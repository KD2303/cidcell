# Testing

## Backend
- Testing library: `jest` and `supertest` configured in package.json.
- Run tests via `npm test`.

## Frontend
- No explicit automated tests configured standard out of the box (e.g., vitest/jest). Manual testing primarily.
